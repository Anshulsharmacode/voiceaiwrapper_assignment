[project]
name = "voiceaiwrapper"
version = "0.1.0"
description = "Django project with PostgreSQL database (Neon)"
requires-python = ">=3.8"
dependencies = [
    "Django==4.2.27",
    "psycopg2-binary==2.9.9",
    "python-decouple==3.8",
]

[tool.hatch.build.targets.wheel]
packages = []